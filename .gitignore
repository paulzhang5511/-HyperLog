/target

# 打包产物（scripts/package_macos.sh 产出：HyperLog.app / *.zip）
/dist/

# 工作台数据（记忆、会话状态），不入库
/.workbuddy/

# 本地生成的大样本日志（scripts/gen_log.sh 产出目录）
/tmp/
/bench/

# 本地测试日志样本（scripts/gen_sample_logs.py 产出，含 1GB 压测文件，不入库）
/sample_logs/

# 导出过程中的临时文件
*.log.tmp

.DS_Store
